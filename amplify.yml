version: 1
frontend:
  phases:
    build:
      commands:
       - 'hugo --gc --minify'
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths:
      - .hugo/**/*
      - node_modules/**/*
      - public/**/*